import { TextInputProps } from "react-native";
import { Container } from "./styles";

export function Input({}: TextInputProps) {
    return(
        <Container>
            
        </Container>
    );
}
